git add .
git commit -m "Update index.html"
git push


Now when someone downloads the repo, they just need to:
1. pip install -r requirements.txt
2. Copy .env.example to .env and add their Lark credentials
3. python setup_mysql.py
4. python app.py